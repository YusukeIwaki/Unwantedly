<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionModeOverlay">true</item>

        <item name="colorPrimary">@color/cw_blue_primary</item>
        <item name="colorPrimaryDark">@color/cw_blue_primary_dark</item>
        <item name="colorAccent">@color/cw_accent</item>

        <!-- Lollipop以降の端末向けのオマケ -->
        <item name="android:statusBarColor" tools:targetApi="21">?attr/colorPrimaryDark</item>
        <item name="android:navigationBarColor" tools:targetApi="21">?attr/colorPrimaryDark</item>
    </style>

    <style name="CWActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">?attr/colorPrimary</item>
    </style>

</resources>
